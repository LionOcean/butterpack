const config = require("../../../butterpack.config");

export const entry = config.entry;
export const staticDir = config.staticDir;
export const loaders = config.loaders;
export const fileExts = config.fileExts;